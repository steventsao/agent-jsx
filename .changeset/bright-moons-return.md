---
"@agent-jsx/core": minor
---

Replace the short-lived `agent({ render })` factory with PascalCase function components that return JSX directly and are lowered with an explicit profile through compiler-generated companions.
