{
  "name": "agent-jsx-compat-cloudflare",
  "private": true,
  "type": "module",
  "imports": {
    "#agentjsx/*": "./src/generated/runtime/*.ts"
  },
  "description": "Proves generated artifacts run on the REAL cloudflare/agents package inside workerd (vitest-pool-workers) — no mocks, no dev server.",
  "scripts": {
    "generate": "bun scripts/generate.tsx",
    "generate:deploy": "bun scripts/generate-deploy.tsx",
    "typecheck": "bun run generate && bun run generate:deploy && tsc --noEmit",
    "test": "bun run generate && vitest run"
  },
  "dependencies": {
    "agents": "^0.8.5",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "@cloudflare/workers-types": "^4.20260317.1",
    "@types/react": "^19.0.0",
    "typescript": "^5.7.0",
    "vitest": "4.1.1",
    "wrangler": "^4.77.0"
  }
}
