{
  "name": "agent-jsx-pdf-target",
  "private": true,
  "type": "module",
  "description": "Phase A2: the HAND-WRITTEN cloudflare/agents target for the PDF pipeline, proven in real workerd and on a live deploy (agent-jsx-pdf). See ../../PDF-PIPELINE.md.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "agents": "0.17.4",
    "unpdf": "^1.3.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "^5.7.0",
    "vitest": "4.1.1",
    "wrangler": "^4.77.0"
  }
}
